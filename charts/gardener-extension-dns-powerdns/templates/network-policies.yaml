{{- if .Values.networkPolicies.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-to-powerdns
  namespace: {{ .Release.Namespace }}
spec:
  egress:
  - ports:
    - port: 8081
      protocol: TCP
    to:
    - podSelector:
        matchLabels:
          {{- .Values.networkPolicies.powerDNS.podSelector | toYaml | nindent 10 }}
      namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: {{ .Values.networkPolicies.powerDNS.namespace }}
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "name" . }}
  policyTypes:
  - Egress
{{- end }}
